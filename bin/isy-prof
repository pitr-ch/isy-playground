#!/usr/bin/env ruby

require "rubygems"
require "bundler"
require 'ruby-prof'
Bundler.setup
require "isy"

result = RubyProf.profile do
  Isy::Runner.run!
end

File.open 'profile.html', 'w' do |f|
  RubyProf::GraphHtmlPrinter.new(result).print(f)
end
